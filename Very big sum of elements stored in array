// Very big sum of elements stored in array.
#include<iostream>
using namespace std;
int main()
{
    long long int a[10],n,i,sum=0;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(i=0;i<n;i++)
    {
        sum=sum+a[i];
    }

    cout<<sum;
    return 0;

}
